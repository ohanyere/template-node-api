apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: __SERVICE_NAME__
spec:
  replicas: 1
  strategy:
    canary:
      steps:
        - setWeight: 100
  template:
    spec:
      containers:
        - name: api
          env:
            - name: LOG_LEVEL
              value: debug
          resources:
            requests:
              cpu: 50m
              memory: 96Mi
            limits:
              cpu: 250m
              memory: 256Mi
